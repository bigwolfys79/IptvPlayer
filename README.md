# IptvPlayer

IPTV-плеер для плейлистов M3U/M3U8 с программой передач (EPG), архивом (timeshift) и полноценным HEVC/AC-3 воспроизведением на базе FFmpeg. WinUI 3 / .NET 8 / Windows App SDK.

- **Версия:** 1.7.1
- **Настройки и кэш:** `%LocalAppData%\IptvPlayer`
- **Лог (Serilog):** `%LocalAppData%\IptvPlayer\logs` (ежедневный роллинг, включается/выключается в настройках)
- **Сборка Debug:** MSIX (F5 в Visual Studio, Platform=x64)
- **Сборка Release + инсталятор:**
  ```
  dotnet publish IptvPlayer.csproj -c Release -p:PublishProfile=win-x64
  "%LOCALAPPDATA%\Programs\Inno Setup 7\ISCC.exe" installer\IptvPlayer.iss

или & "$env:LOCALAPPDATA\Programs\Inno Setup 7\ISCC.exe" installer\IptvPlayer.iss
  ```
  Готовый файл: `installer\output\IptvPlayer-Setup-<версия>-x64.exe`

---

## Реализовано

### Воспроизведение
- **FFmpegInteropX + FFmpeg** — демуксинг и декодирование любых потоков (включая HEVC в MPEG-TS и AC-3, которые системный плеер Windows не тянет); рендер — штатный MediaPlayerElement.
- Режимы декодирования видео, переключаются в настройках: аппаратный (GPU с откатом на CPU) и программный (CPU, по умолчанию — плавно даже на 1080p60).
- Упреждающая буферизация (15 с / 32 МБ) — устраняет замирания на стыках 10-секундных HLS-сегментов.
- Громкость: слайдер в панели управления, колесо мыши над видео, сохраняется между запусками; многоканальный звук (5.1) сводится аудиодвижком Windows (не тихий downmix FFmpeg).
- **Режимы отображения видео**: вписать (letterbox, по умолчанию) / растянуть / обрезать (масштаб с сохранением пропорций, выступающее за края отсекается) — для контента не в пропорции окна (4:3 на 16:9); кнопка в обеих панелях управления и клавиша V, текущий режим — в подсказке кнопки, выбор сохраняется в настройках.
- **Оверлей статистики (Ctrl+J, как в VLC)**: кодеки видео/аудио, разрешение, fps, битрейты, фактический декодер видео (FFmpeg D3D11 GPU / FFmpeg CPU / системный — как выбрал FFmpegInteropX, включая откат в аппаратном режиме) и статус аппаратного декодера на машине, глубина буфера, число простоев буферизации и время сессии канала (живая строка — тикает каждую секунду) — для диагностики «тормозит» достаточно скриншота оверлея; работает в оконном и полноэкранном режимах (в fullscreen — под шапкой с названием канала, правее списка каналов). Включается/выключается также тумблером «Статистика потока» в настройках (Диагностика), состояние переживает перезапуск.
- **Горячие клавиши и беззвучный режим**: пробел — пауза/продолжение архива; ↑/↓ и PgUp/PgDn — соседний канал по кругу в пределах текущего фильтра (в полноэкранном режиме оверлей показывается, чтобы было видно, куда переключились); M — без звука с кнопками mute в обеих панелях управления (слайдеры показывают ноль, громкость восстанавливается при снятии); F/F11 и двойной клик по видео — полный экран; Esc — выход из него; Ctrl+F — фокус в поиск с выделением текста; V — режим отображения; Ctrl+J — статистика потока; цифры — прямой ввод номера канала с оверлеем как в телевизоре (Enter или 3 с — коммит, Backspace — стереть, Esc — отмена). Буквенные клавиши работают в любой раскладке (VK-коды); в полях ввода и открытых диалогах горячие клавиши не действуют; стрелки/PageUp/PageDown не перехватываются, когда фокус на списке, слайдере перемотки или комбобоксе.
- Архив (timeshift): клик по передаче в EPG запускает её с начала; кнопки «В эфир» и пауза появляются в панели управления во время архива; EPG-оверлей автоматически скрывается при запуске передачи.
- **Перемотка архива**: полоса с подписями времени в панелях управления (оконной и полноэкранной) во время архивного показа — вся полоса изображает передачу целиком (ноль — её начало), отпускание ползунка перезапускает поток с новой точки (HLS-timeshift не ищется на лету); позиция и паузы считаются по часам, после перемотки индикатор остаётся на выбранной минуте, дальше живого эфира уйти нельзя.
- **Прогресс текущей передачи**: тонкая полоса в строке канала (главный список и полноэкранный оверлей — общий шаблон), в карточке текущей передачи EPG и в шапке полноэкранного оверлея — видно, сколько передачи уже прошло; обновляется каждые 30 с.
- Стабильность: источник FFmpeg привязан ко времени жизни плеера (ConditionalWeakTable) — устранён крах 0xC00D36B6 с пропаданием звука/видео; ошибки плеера пишутся в лог с кодами (MediaPlayer.MediaFailed).

### EPG и сопоставление каналов
- Загрузка и слияние XMLTV-источников (основной — полный фид `epg.one/epg.xml.gz`), автообновление по расписанию из настроек (1/3/7 дней или только вручную).
- Таблица соответствий «имя → tvg-id» из сервиса epg.one/setup-playlist (`Assets/epg-name-map.json`, 3218 записей): надёжное сопоставление по id для ~2043 из 2065 каналов (98%), включая правильные таймшфт-расписания («Первый канал +2» имеет собственный id).
- Резервное сопоставление по нормализованным именам: срезание таймшфта `+N`, кодов стран, маркеров потока (orig/50/60/hdr), «international/международный», алиасы (кинозал→кино, tviksel-ключи), детерминированный выбор для неоднозначных имён.
- Покрытие поднято с 575 (28%) до ~2043 каналов; остальные либо отсутствуют в EPG-источниках, либо это внутренние каналы Триколора.
- Логотипы каналов в списке: из `<icon>` XMLTV и tvg-logo таблицы epg.one.
- «Текущая передача» в списке каналов обновляется таймером каждые 30 секунд, а не только при клике.
- В карточках EPG ко времени добавляется день для передач не-сегодня: «Вчера 12:00», «Завтра 12:00», более дальние дни — «14.08, 12:00» — без этого по одному времени непонятно, за какой день программа.
- **Быстрый кэш EPG**: MemoryPack + Brotli вместо JSON — файл кэша 280 МБ → 9,5 МБ, чтение 420k программ < 1 с, готовый EPG через ~2 с после запуска; автоматическая миграция со старого JSON-кэша без перекачки; версионирование формата.

### Интерфейс
- Видео занимает всю правую область; EPG — перекрывающий оверлей, открывается/закрывается кнопкой в панели управления.
- Панель управления видео (громкость, пауза архива, «В эфир», EPG, настройки, полноэкранный режим) появляется по движению мыши и скрывается сама через 3 с; работает в оконном и полноэкранном режимах; исправлено появление «только после остановки мыши».
- Убраны: постоянная панель над плеером, шапка EPG с комбобоксом источника и кнопкой «Обновить EPG», баннер архива поверх видео.
- Диалог настроек: источники EPG, URL плейлиста, периодичности обновления, режим декодера; «Готово» сохраняет всё (отдельная кнопка «Сохранить» убрана); «Сбросить» с двухшаговым подтверждением удаляет источники EPG и плейлист (включая кэш и список каналов); «О программе» — версия, движок, пути.
- Список каналов отображается сразу при запуске, не дожидаясь загрузки EPG; весь EPG-путь (кэш, парсинг, слияние, индексация) выполняется вне UI-потока.
- Индикатор архива передач в списке каналов: зелёная точка у каналов с архивом (атрибут `tvg-rec`/`catchup-days` плейлиста, у этого провайдера — 906 из 2065 каналов), серая — без архива; подсказка показывает глубину архива в днях.
- **Напоминания о передачах**: колокольчик в карточке будущей передачи в EPG → тост Windows за N минут до начала (настраивается в настройках, по умолчанию 5; `CommunityToolkit.WinUI.Notifications`). Напоминания переживают перезапуск, устаревшие чистятся автоматически.
- **Избранные каналы**: звёздочка в списке (закрашенная/контурная), избранные всегда наверху списка и в группе «★ Избранное» полноэкранного оверлея, есть фильтр по избранному; хранятся по имени канала в настройках.
- **Автопродолжение**: при запуске выбирается и включается последний смотренный канал; восстанавливается и его группа в фильтре списка (канал виден в своём подразделе), список прокручивается к нему.
- **Тёмная тема**: Светлая/Тёмная/Системная через FrameworkElement.RequestedTheme — применяется на лету (на старте и сразу при смене в настройках).
- **Язык интерфейса** (Русский/English): локализатор Services/Localizer.cs, пары строк в местах использования; переключение действует сразу.
- **Слайдер буфера видео** в настройках (5–60 с): управляет ReadAheadBuffer FFmpeg — плавность против задержки от эфира.
- **Живой разделитель панелей**: ширина списка каналов тянется мышью (CommunityToolkit Sizers, 240–640 px) и запоминается.
- **Память окна**: позиция, размер и развёрнутость окна восстанавливаются при запуске (нативный AppWindow.MoveAndResize, с вписыванием в рабочий стол при отключённом мониторе).
- **MVVM-рефакторинг (этапы 1–2 завершены)**: ViewModel'и (`MainPageViewModel`, `EpgViewModel`, `PlayerViewModel`, `ChannelViewModel`) на `ObservableObject` со свойствами через `SetProperty` — `[ObservableProperty]` сознательно не используется (генератор не создаёт WinRT-проекторов — MVVMTK0045, важно для AOT/ABI). Действия пользователя — на `[RelayCommand]`; кнопки в XAML привязаны через `Command=`, `ItemClick` списков и клики из DataTemplate — тонкими обёртками в code-behind, вызывающими те же команды. Диалог настроек вынесен из MainPage в `Dialogs/SettingsDialog` (разметка — XAML, логика — code-behind контрола; MainPage сократился с ~1950 до ~1360 строк); в code-behind MainPage остались полноэкранный режим, видео-оверлеи, таймеры, тема/язык и диалог добавления канала. Мёртвые команды постраничной навигации EPG (PrevDay/NextDay/Today) удалены.
- **Запись передач и каналов**: кнопка записи текущего канала в панели управления (ffmpeg -c copy в ВидеоIptvPlayer, до повторного нажатия) и кнопка записи на будущих передачах в EPG (расписание, старт по таймеру, работает пока приложение запущено).
- **Serilog + DI**: вместо самописного файлового логгера — Serilog (Debug-вывод + файл в `%LocalAppData%\IptvPlayer\logs` с ежедневным роллингом, 14 дней хранения; тумблер в настройках действует на лету), классы получают `ILogger<T>` конструктором; зависимости собирает `Microsoft.Extensions.DependencyInjection` (composition root в `App`, `App.Services`) — все `?? new …`-фолбэки и разрозненные `new SettingsService()` убраны, сервисы и ViewModel'ы — singletons.
- Записи хранятся в «ВидеоIptvPlayer» в формате MPEG-TS без перекодирования.
- Иконка приложения (окно, инсталятор, все MSIX-логотипы) — `Assets/AppIcon.ico`; генератор логотипов: `analysis/gen-icons.ps1`.
- Приложение закрывается по крестику мгновенно (остановка плеера + немедленный выход процесса).

### Инфраструктура
- Release собирается unpackaged (`WindowsPackageType=None` + `WindowsAppSDKSelfContained`) под классический установщик; Debug остаётся MSIX для F5.
- Инсталятор Inno Setup (`installer/IptvPlayer.iss`): мастер с выбором языка установки (русский/английский — диалог выбора при запуске), иконки, lzma2, встроенные декодеры Dolby AC-3/AC-4 (тихо ставятся при отсутствии, с проверкой).
- `installer/Install-DolbyDecoders.ps1` — автономная установка декодеров Dolby (Microsoft Store → при отказе региона: сайдлоад с зеркала).
- Скрипты в `analysis/`: генерация `epg-name-map.json` из m3u8 epg.one (gen-map.mjs), офлайн-анализ покрытия сопоставления (match-analysis.mjs), генератор иконок, TS-пробер кодеков, измерения громкости/скорости.
- Настройки и кэш в `%LocalAppData%\IptvPlayer` (работает и в MSIX, и unpackaged); файловый лог с ротацией.

---

## К реализации (задачи)

Черновой порядок — по польза/усилия; `[x]` — сделано.

### Воспроизведение и UX
- [x] **Верхний оверлей в оконном режиме** — шапка над видео с названием канала, текущей передачей, прогрессом и индикатором архива (как верхняя панель полноэкранного оверлея, но для окна); живёт строго внутри области видео (RightPanelGrid, за списком каналов и разделителем) и не перекрывается им; сейчас эта информация в оконном режиме есть только в списке каналов слева.
- [x] **Горячие клавиши** — пробел (пауза архива), ↑/↓ и PgUp/PgDn (соседний канал по кругу), M (без звука + кнопки mute в панелях), F/F11 и двойной клик по видео (полный экран), Esc (выход из полноэкранного), Ctrl+F (фокус в поиск), цифры — прямой ввод номера канала с оверлеем как в телевизоре. *(1.4.13)*
- [x] **Режимы отображения видео** — letterbox/растянуть/обрезать для контента не в пропорции окна. *(1.4.14)*
- [x] **Оверлей статистики (Ctrl+J)** — кодек, разрешение, битрейт, фактический декодер (GPU/CPU после отката), простои буферизации. *(1.4.14)*
- [x] **Таймер сна** — остановка воспроизведения через N минут.
- [x] **Мини-плеер поверх окон** — Ctrl+M: компактное always-on-top окно 480x300 без панелей.
- [x] **Трей** — крестик сворачивает в трей, звук продолжает играть; нативный Shell_NotifyIcon (H.NotifyIcon несовместим с текущей версией WinAppSDK). Отключается в настройках интерфейса.

### Записи
- [x] **Список записей в UI** — меню шестерёнки → «Записи»: активные (стоп) и запланированные (убрать), настраиваемая папка с автосозданием.
- [x] **Записи, переживающие перезапуск** — активные записи запоминаются при закрытии; при следующем запуске, если передача ещё идёт, предлагается продолжить запись оставшейся части (новый файл «… (продолжение)», свежий URL потока).
- [x] **Аккуратная остановка ffmpeg** — `q` в stdin, Kill как запасной путь после 3 с.

### Инфраструктура и качество
- [x] **Git-репозиторий** — инициализирован; `bin/`, `obj/`, `.vs/`, `AppPackages*`, тяжёлые бинарники `analysis/`/`installer/` в `.gitignore`.
- [x] **Юнит-тесты** — `tests/IptvPlayer.Tests` (xunit): нормализация имён EPG, `M3UParserService.ParseContent` (атрибуты, tvg-rec/catchup-days, #EXTGRP), `ArchiveUrlBuilder`. Запуск: `dotnet test tests/IptvPlayer.Tests -p:Platform=x64 -p:WindowsAppSdkAutoInitialize=false` (флаг отключает автоинициализацию WinAppSDK в тестовом хосте).
- [x] **Этап 3 MVVM (частично)** — полноэкранный режим/оверлеи/горячие клавиши/оверлей статистики вынесены из MainPage в partial-файлы (`MainPage.FullScreen.cs`, `MainPage.Overlays.cs`, `MainPage.Hotkeys.cs`, `MainPage.StatsOverlay.cs`); SettingsDialog разбит на partial по зонам, из EPGService вынесены EpgNameNormalizer и EpgSourceMerger. Ядро MainPage — ~1900 строк.
- [x] **Несколько плейлистов** — переключение через меню настроек, избранное глобальное по имени канала.
- [x] **Автообновление** — «О программе» → «Проверить обновления»: JSON с UpdateCheckUrl из settings.json, кнопка «Скачать обновление».
- [x] **«Открыть папку логов»** — кнопка в «О программе»; папка логов также указана в настройках интерфейса.
- [x] **Родительский контроль** — каналы остаются в списке, но запуск каналов выбранных групп (автоподбор «взрослых» + вручную) требует PIN (PBKDF2); при вводе PIN предлагается отключить запрос на 15/30/45/60 мин или до выключения; пункт «Родительский контроль» в меню настроек.
- [x] **«Предыдущий канал» + недавно смотренные** — сессионная история: кнопка «←» в обеих панелях и Backspace.
- [x] **Настраиваемая папка записей** — в диалоге «Дополнительно» → «Папка записей»; кнопка «Записи» со списком активных/запланированных и «Открыть папку».
- [x] **Несколько параллельных записей** — до 3 одновременно (лимит сессий провайдера); расписание стартует не дожидаясь слота.

---

## Известные особенности

- Задержка от живого эфира ~10–15 с (плата за упреждающую буферизацию против замираний на стыках сегментов; при необходимости уменьшить `ReadAheadBufferDuration` в `StreamService`).
- ~20 каналов без EPG: внутренние киноканалы Триколора («Кинозал 3–12»), сезонные и технические каналы — их расписаний нет ни в одном источнике.
- MSIX-версия (Debug) и Inno-версия — «разные приложения» с общими настройками; удаляйте одну, чтобы не путаться в ярлыках.
- Файловый лог по умолчанию включён и пишется в `%LocalAppData%\IptvPlayer\logs` (ежедневный роллинг); выключается тумблером «Файловый лог» в настройках — действует сразу, вывод в Debug (окно Output) остаётся всегда.
