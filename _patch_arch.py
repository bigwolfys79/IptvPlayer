# -*- coding: utf-8 -*-
def patch(path, old, new):
    src = open(path, encoding="utf-8").read()
    assert src.count(old) == 1, (path, old[:60])
    open(path, "w", encoding="utf-8", newline="\n").write(src.replace(old, new))

A = "ARCHITECTURE.md"

# Ключевые классы: добавить новые
patch(A,
"""- `Services/EPGService` — слияние источников, индексы сопоставления, логотипы.
- `Assets/epg-name-map.json` — таблица «имя → tvg-id» сервиса epg.one/setup-playlist.
- `MainPage` (+ `ViewModels/MainPageViewModel`, `EpgViewModel`, `ChannelViewModel`) — весь UI и оверлеи.
- `Services/StreamService` — создание плеера (FFmpegInteropX).""",
"""- `Services/EPGService` — слияние источников и координация; чистая логика вынесена в `Services/EpgNameNormalizer` (нормализация имён) и `Services/EpgSourceMerger` (слияние источников + индекс имён).
- `Assets/epg-name-map.json` — таблица «имя → tvg-id» сервиса epg.one/setup-playlist.
- `MainPage` (+ `ViewModels/MainPageViewModel`, `EpgViewModel`, `ChannelViewModel`) — весь UI и оверлеи; code-behind разбит на partial-файлы по зонам (FullScreen/Overlays/Hotkeys/StatsOverlay).
- `Services/StreamService` — создание плеера (FFmpegInteropX).
- `Services/RecordingService` — параллельные записи через ffmpeg.exe (до 3, остановка по `q`).
- `Services/TrayIconService` — иконка в трее (нативный Shell_NotifyIcon).
- `Services/ParentalControlService` — PIN (PBKDF2) и временная разблокировка запуска каналов групп.
- `Services/ChannelHistory` — сессионная история для «предыдущего канала».""")

# Парсер: без конкретики провайдера
patch(A,
"и признаки архива: `tvg-rec`, `catchup-days`, `catchup` (у этого провайдера — `tvg-rec=\"7\"`; нечисловое значение типа `default` трактуется как 1 день).",
"и признаки архива: `tvg-rec`, `catchup-days`, `catchup` (нечисловое значение типа `default` трактуется как 1 день). Скачивание принимает CancellationToken — переключение плейлиста отменяет идущую загрузку.")

# Источники EPG: без размеров конкретного фида
patch(A,
"**Источники** — список URL XMLTV в настройках (основной: полный фид `epg.one/epg.xml.gz`, 45 МБ gzip / 420 МБ XML / ~420 тыс. передач). `XmlTvService.LoadAsync`:",
"**Источники** — список URL XMLTV в настройках (по умолчанию — полный фид `epg.one/epg.xml.gz`; полный фид — десятки МБ gzip и сотни тысяч передач). `XmlTvService.LoadAsync` (токен отмены прокинут до HttpClient):")

patch(A,
"Чтение 420 тыс. передач занимает < 1 с (JSON занимал ~5–8 с при 280 МБ).",
"Чтение сотен тысяч передач занимает < 1 с (старый JSON-кэш читался в разы дольше и был в десятки раз больше).")

# Слияние — актуальный класс
patch(A,
"**Слияние источников** (`EPGService.MergeSources`, пул потоков):",
"**Слияние источников** (`EpgSourceMerger.Merge`, пул потоков):")

# Сопоставление: без конкретики провайдера и чисел
patch(A,
"Плейлист этого провайдера **вообще не содержит tvg-id**, поэтому работают четыре пути — от надёжного к приблизительному (`EPGService.MatchChannel`):",
"Плейлисты ряда провайдеров **не содержат tvg-id**, поэтому работают четыре пути — от надёжного к приблизительному (`EPGService.MatchChannel`):")

patch(A,
"2. **Таблица epg.one** (`Assets/epg-name-map.json`, генерируется из m3u8 сервиса epg.one/setup-playlist, 3218 записей): **строгий ключ** — нормализованное имя с сохранённым таймшифтом («Первый канал +2» имеет собственный tvg-id → корректное расписание со сдвигом);",
"2. **Таблица epg.one** (`Assets/epg-name-map.json`, генерируется из m3u8 сервиса epg.one/setup-playlist): **строгий ключ** — нормализованное имя с сохранённым таймшифтом (таймшифт-версии каналов имеют собственные tvg-id → корректное расписание со сдвигом);")

patch(A,
"**Нормализация** (`NormalizeChannelNameCore`):",
"**Нормализация** (`EpgNameNormalizer.Normalize/NormalizePreservingTimeshift/NormalizeKeepQualifiers`):")

patch(A,
"""- для брендовых префиксов («Tviksel Кино 2 HD» → ключ «кино 2») добавляются дополнительные ключи, только если не заняты прямыми именами.

Итог по текущему плейлисту: ~2043 из 2065 каналов с EPG (98%); остальные (Триколор-«Кинозалы», сезонные) расписаний не имеют ни в одном источнике.""",
"""- для брендовых префиксов («Tviksel Кино 2 HD» → ключ «кино 2») добавляются дополнительные ключи, только если не заняты прямыми именами.

Итог сопоставления по всему плейлисту пишется в лог одной сводкой (сопоставлено по id / по таблице / по имени / не сопоставлено) — по ней видно покрытие конкретного плейлиста.""")

# Логотипы: без конкретики
patch(A,
"""1. `tvg-logo` из плейлиста (этот провайдер не отдаёт — всегда пусто);
2. `<icon src>` из XMLTV (по tvg-id);
3. `tvg-logo` из таблицы epg.one (`_logoByTvgId` — тот же файл, из которого берутся tvg-id; 3218 логотипов на epg.one/img/…).""",
"""1. `tvg-logo` из плейлиста (часть провайдеров его не отдаёт);
2. `<icon src>` из XMLTV (по tvg-id);
3. `tvg-logo` из таблицы epg.one (`_logoByTvgId` — тот же файл, из которого берутся tvg-id).""")

# Архив: без чисел
patch(A,
"**Признак.** Глубина архива берётся из `tvg-rec`/`catchup-days` плейлиста (906 из 2065 каналов: 3 или 7 дней) и хранится в `ChannelViewModel.CatchupDays`.",
"**Признак.** Глубина архива берётся из `tvg-rec`/`catchup-days` плейлиста и хранится в `ChannelViewModel.CatchupDays`.")

# Воспроизведение: добавить HLS-опции; обновить закрытие (трей)
patch(A,
"   - упреждающая буферизация 15 с / 32 МБ — сглаживает стыки 10-секундных HLS-сегментов (платой является задержка от эфира ~10–15 с).",
"""   - упреждающая буферизация (по умолчанию 15 с / 32 МБ, настраивается) — сглаживает стыки 10-секундных HLS-сегментов (платой является задержка от эфира ~10–15 с);
   - HTTP-опции FFmpeg: `http_persistent=0` (keepalive сегментов отключён — провайдеры, чередующие сервера сегментов, ломают переиспользуемое соединение на каждом сегменте, что давало затыки) и `reconnect*` — авто-восстановление при обрывах.""")

patch(A,
"""**Закрытие приложения**: подписка на `MainWindow.Closed` останавливает/освобождает плеер и вызывает `Environment.Exit(0)` — иначе медиа-конвейер держал процесс живым несколько секунд после закрытия окна.""",
"""**Закрытие приложения**: по крестику окно сворачивается в трей (`TrayIconService`, нативный Shell_NotifyIcon — воспроизведение продолжается; полный выход через меню иконки, отключается в настройках). Настоящее закрытие: `AppWindow.Closing` убирает иконку трея, подписка на `MainWindow.Closed` останавливает/освобождает плеер и записи, запоминает идущие записи для предложения продолжения при следующем запуске (`InterruptedRecording`), и вызывает `Environment.Exit(0)` — иначе медиа-конвейер держал процесс живым несколько секунд после закрытия окна.""")

# Данные на диске: добавить настройки записи/плейлисты
patch(A,
"| Настройки (источники, периодичности, громкость, режим декодера) | `%LocalAppData%\\IptvPlayer\\settings.json` |",
"| Настройки (источники, периодичности, громкость, режим декодера, плейлисты, записи, родительский контроль) | `%LocalAppData%\\IptvPlayer\\settings.json` |")

# Новая секция про прочие подсистемы — перед §9
patch(A,
"## 9. Данные на диске",
"""## 9. Записи, родительский контроль, обновления

- **Записи** (`RecordingService`): внешний `ffmpeg.exe -c copy -f mpegts` (лежит рядом с приложением, DLL кодеков — те же, что FFmpegInteropX кладёт в вывод); до 3 параллельных процессов, словарь по `Guid`, событие `RecordingsChanged` обновляет UI; остановка — `q` в stdin (ffmpeg корректно дописывает TS), Kill как запасной путь. Папка настраивается (`RecordingsFolder`, по умолчанию «Видео\\IptvPlayer»). Расписание (`ScheduledRecording`) персистится в настройках и проверяется 30-секундным таймером; каналы разрешаются по имени (URL потоков содержит недолговечные подписи). Прерванные закрытием записи (`InterruptedRecording`) предлагается продолжить при следующем запуске — свежий URL, новый файл «… (продолжение)».
- **Родительский контроль** (`ParentalControlService` + `Dialogs/ParentalControlDialog`): группы в блок-листе выбираются вручную (при включении автопроверяются «взрослые» по ключевым словам); каналы остаются в списке, но их запуск (`MainPageViewModel.EnsureChannelAllowedAsync`, включая архив и автопродолжение) требует PIN. PIN хранится PBKDF2-хэшем; разблокировка на 15/30/45/60 мин или до выключения (`ParentalControlUnlockedUntilUtc`).
- **Обновления** (`Dialogs/AboutDialog`): «Проверить обновления» → GitHub API `releases/latest` репозитория проекта (или свой `UpdateCheckUrl` с JSON version/url); тег больше текущей версии → кнопка «Скачать обновление» открывает установщик релиза в браузере.
- **Единая палитра**: акцентные цвета (REC, эфир, архив, избранное, выделение канала, скримы оверлеев) — именованные ресурсы в `App.xaml`; стиль оверлейных кнопок — `OverlayIconButtonStyle` (прозрачный фон, белая подсветка при наведении).

## 10. Данные на диске""")

# Перенумерация двух последних секций
patch(A, "## 10. Логирование и DI", "## 11. Логирование и DI")

print("ARCHITECTURE patched")
